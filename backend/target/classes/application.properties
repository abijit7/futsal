# ============================
# Futsal Booking System Config
# ============================

# Database


# Server
# Use a non-DB port for the Spring Boot server. 3306 is MySQL's default port and
# will cause a port collision if the DB is on the same host. Use 8080 (default)
server.port=8080

# CORS is handled in CorsConfig.java
spring.datasource.url=jdbc:mysql://localhost:3306/futsal_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Aabbiijjiitt112233#

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect