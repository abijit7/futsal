# Development profile
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/futsal_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

management.endpoint.health.show-details=always

